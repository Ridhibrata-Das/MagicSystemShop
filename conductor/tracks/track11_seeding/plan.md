# Track 11 Plan

## Phase 1: Seed Implementation

- [x] Task 1.1: Create `app/api/seed/route.ts` as a server-side route handler to insert mock data into Firestore.
- [x] Task 1.2: Generate the mock array of 50 products locally using Unsplash source image placeholders.
- [x] Task 1.3: Trigger the API route once via `fetch` during a dev build (or execute via `curl` to seed DB).
