# Track 15 Plan

## Phase 1: Core Theming & Globals
- [x] Task 1.1: Override `tailwind.config.ts`, `globals.css`, and import a custom futuristic Font inside `layout.tsx`.
- [x] Task 1.2: Build a generic `<SystemWindow>` wrapper UI component that dictates layout styles (borders, glow, transluency).
- [x] Task 1.3: Build a custom `SystemMessage` Context/Toast hook to fire game-like notifications.

## Phase 2: Refactoring Components
- [x] Task 2.1: Update `ProductCard` into an RPG item panel.
- [x] Task 2.2: Redesign the `app/page.tsx` structural flow (Shop Categories left, Items Grid main) inside of specific System Windows.
- [x] Task 2.3: Redesign `app/cart/page.tsx` as an Inventory table with specific UI slots.
- [ ] Task 2.4: Convert Auth, Checkout, and Profile pages into "CONFIRM PURCHASE" and "PLAYER STATUS" stylized System Windows.
